# Deploy app to Kubernetes (k8s) environment #

Application is simple container that prints its build id and its hostname.

## Create Docker image ##

Dockerfile:
```
FROM
```

## Deploy app to k8s cluster ##

Use `kubectl` to define and deploy application to k8s.
